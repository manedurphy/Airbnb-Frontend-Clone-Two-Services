import React from 'react';
import LeftContainer from '../../../styled-components/LeftContainer';

const LoadingLeft = () => (
    <LeftContainer className={'photo-container loading-photo'} />
);

export default LoadingLeft;
